
import { Card, Typography } from 'antd';

const { Title } = Typography;

const Repository = () => {
  return (
    <Card style={{ margin: 20 }}>
      <Title level={2}>Bhumika Repo Yaha add krio</Title>
    </Card>
  );
};

export default Repository;
